# 18. 収集日画面（F3）

> 関連: `REQUIREMENTS.md` §2.1 F3, §6.1
> ステータス: ✅ 完了（2026-05-17）

## 目的

ユーザーの地区における、各カテゴリの **次回収集日** とこの先2〜4週の予定を一覧表示する。タブの2番目。

## 完了条件

- カテゴリ別の次回収集日が一目でわかる
- 「今日」「明日」「明後日」が特別表示される
- 地区未設定時はメッセージ + 設定画面への誘導

## Todo

### 基本実装

- [×] `app/(tabs)/schedule.tsx` 作成
- [×] [[08_storage_layer]] から地区ID取得（`useUserSettings()` 経由）
- [×] [[07_data_loader]] から patterns / areas / categories を取得（`useData()` 経由）
- [×] 該当地区の `patternId` を引いて pattern を特定

### 表示

- [×] カテゴリごとのカード（カテゴリ色付き、`categories.json` の `color` を流し込み）
  - 燃やすごみ・プラスチック資源・埋立ごみ・有害ごみ・金属類資源・紙類資源
- [×] 各カードに「次回: ○月○日（○）」、距離（「明日」「3日後」など）（`formatNextCollection`）
- [×] 当日の朝7時前なら「今日」、朝7時以降なら「次回」を表示（`COLLECTION_CUTOFF_HOUR=7` で対応済み）
- [ ] カードタップでそのカテゴリの詳細・施設情報へ（任意 / 16・19 完成後に検討）

### 予定一覧

- [×] この先2〜4週分の予定をリストで（4 週間、`getCollectionsInRange` + `groupByWeek` で算出）
- [×] 月またぎでも自然に表示（週見出しの "M/d 〜 M/d" 表示で吸収）

### 通知連携

- [×] 通知設定 ON 状態を表示（Switch を直接配置）
- [×] 「明日のごみ出しを通知する」スイッチへの導線（本画面に常設、設定値のみ永続化）
  - 実通知スケジュール登録は [[12_notifications]] の責務

### 地区情報

- [×] 上部に現在地区名・パターン説明（`patterns.json` の `description`）
- [×] 地区変更ボタン → 設定画面（"変更" ピル）

### 未設定時

- [×] 地区IDがない場合のフォールバック（オンボーディングへ）（`app/(tabs)/_layout.tsx:13` で Redirect 済み）
- [×] No.36 など `TBD_NEEDS_VERIFICATION` の場合は「データ準備中」メッセージ（`TbdFallback` セクション）

### ベータ版表示

- [×] 「データは令和8年度ベース」「最新はお住まいの地区の案内を」（§8.1）
- [×] 祝日休止は反映されない旨を明記（注意点に従って追記）

## 注意点

- 祝日休みは MVP では考慮しない（[[10_schedule_calculator]]）。**「祝日は別途確認」と添える**
- カテゴリ並びは「直近の日付順」が UX 良し（毎日変わる）
- カレンダー風 UI を凝りすぎない。MVP はリストで十分
