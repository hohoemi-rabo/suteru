# 14. ホーム画面

> 関連: `REQUIREMENTS.md` §6.1, §6.3
> ステータス: 未着手（テンプレートサンプル状態）

## 目的

アプリ起動後のメイン画面。大きなカメラボタンと検索バー、施設・収集日・設定への導線をシンプルに提示する。

## 完了条件

- カメラボタンが画面1/3を占める大きさ
- 上部に検索バー、下部に4タブ（ホーム/収集日/施設/設定）
- 右上に現在地区表示、タップで設定へ
- 起動時に [[07_data_loader]] が完了している

## Todo

### レイアウト

- [ ] `app/(tabs)/_layout.tsx` でタブを4つに整理（index, schedule, facilities, settings）
- [ ] `app/(tabs)/index.tsx` の中身を Welcome テンプレートから差し替え
- [ ] タブアイコン・ラベル（日本語）の設定
- [ ] `screenOptions={{ headerShown: false }}` を tab _layout で指定

### 検索バー

- [ ] 上部に検索 TextInput（プレースホルダ「品目名で探す（例: ペットボトル）」）
- [ ] タップで [[17_manual_search_screen]] へ遷移（または同画面でインライン展開）

### 中央カメラボタン

- [ ] 大型ボタン（画面の1/3、最低 200×200px）
- [ ] カメラアイコン + 「写真でしらべる」テキスト
- [ ] 押下 → `/camera` へ push 遷移（[[15_camera_screen]]）
- [ ] expo-haptics でタップフィードバック

### 現在地区表示

- [ ] 右上に「現在の地区: ○○」表示
- [ ] タップで `/settings` の地区変更へ
- [ ] 地区名が長い場合は省略表示

### 補助コンテンツ

- [ ] ベータ版表示
- [ ] 公式情報リンク（`meta.json` の `officialUrl`）
- [ ] 「次回の収集」をホームに表示するかは MVP では未定（次の収集日タブで見せれば良い）

### データロード

- [ ] [[07_data_loader]] からデータ取得
- [ ] ローディング中は スプラッシュ or プレースホルダ
- [ ] [[08_storage_layer]] から地区ID取得
- [ ] 地区未設定の場合は [[13_onboarding_screens]] にリダイレクト

### アクセシビリティ

- [ ] カメラボタンに `accessibilityLabel`「写真で調べる」
- [ ] 検索バーに `accessibilityLabel`「品目を文字で検索」

## 注意点

- 中央カメラボタンを **目立たせる** のがアプリの体験の核（§6.3）
- React Compiler 有効なので、`useCallback` で props を memoize する必要なし
- タブの並びは「ホーム / 収集日 / 施設 / 設定」が要件指定
